apiVersion: v1
kind: Service
metadata:
  name: prompt-builder-worker
  labels:
    app: prompt-builder
    component: worker
spec:
  type: ClusterIP
  selector:
    app: prompt-builder
    component: worker
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
