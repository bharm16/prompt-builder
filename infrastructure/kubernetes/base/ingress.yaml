apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: prompt-builder-api
  labels:
    app: prompt-builder
  annotations:
    # cert-manager for TLS
    cert-manager.io/cluster-issuer: "letsencrypt-prod"

    # Nginx ingress annotations
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    nginx.ingress.kubernetes.io/ssl-protocols: "TLSv1.2 TLSv1.3"
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
    # Bumped from 60s so SSE/NDJSON streams (label-spans, optimize) survive
    # quiet stretches between events. The label-spans stream emits per-span,
    # but the optimize compile stage can stall for >60s on cold provider
    # warmups; a 60s proxy cutoff would terminate those mid-stream.
    nginx.ingress.kubernetes.io/proxy-read-timeout: "120"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "120"

    # Rate limiting
    nginx.ingress.kubernetes.io/limit-rps: "100"
    nginx.ingress.kubernetes.io/limit-connections: "20"

    # CORS
    nginx.ingress.kubernetes.io/enable-cors: "true"
    nginx.ingress.kubernetes.io/cors-allow-origin: "https://prompt-builder.example.com"
    nginx.ingress.kubernetes.io/cors-allow-methods: "GET, POST, OPTIONS"
    nginx.ingress.kubernetes.io/cors-allow-credentials: "true"

    # Security headers
    nginx.ingress.kubernetes.io/configuration-snippet: |
      more_set_headers "X-Frame-Options: DENY";
      more_set_headers "X-Content-Type-Options: nosniff";
      more_set_headers "X-XSS-Protection: 1; mode=block";
      more_set_headers "Referrer-Policy: strict-origin-when-cross-origin";

spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - api.prompt-builder.example.com
      secretName: prompt-builder-tls
  rules:
    - host: api.prompt-builder.example.com
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: prompt-builder-api
                port:
                  number: 80
