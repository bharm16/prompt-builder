description = "Plan or implement a frontend change using the VideoConceptBuilder pattern."
prompt = """
You are working in the Vidra (PromptCanvas) repo.

Follow the VideoConceptBuilder pattern (see client/src/components/VideoConceptBuilder/):
- Orchestrator component
- hooks/ with useReducer for state
- api/ for fetch calls
- components/ for UI pieces

Heuristics (not hard limits):
- Orchestrator ~500 lines
- UI components ~200 lines
- Hooks ~150 lines

Show the proposed file structure before writing code.
If requirements are unclear, ask concise questions first.
"""
