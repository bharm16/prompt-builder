# Gemini custom commands

These commands are loaded by Gemini CLI from `.gemini/commands/*.toml`.

- frontend   Frontend work using VideoConceptBuilder pattern
- backend    Backend work using PromptOptimizationService pattern
- fullstack  Full-stack changes with explicit API contract
- debug      Hypothesis-driven debugging flow
- tests      Add/update tests (Vitest/Playwright)
