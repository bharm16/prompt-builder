{
  "chromeLaunchConfig": {
    "args": ["--no-sandbox", "--disable-dev-shm-usage"]
  },
  "timeout": 60000,
  "wait": 1000,
  "defaults": {
    "chromeLaunchConfig": {
      "args": ["--no-sandbox", "--disable-dev-shm-usage"]
    },
    "timeout": 60000,
    "wait": 1000
  }
}
